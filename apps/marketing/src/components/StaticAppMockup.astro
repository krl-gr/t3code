<div class="app-mockup" aria-label="Static preview of the Up.computer interface">
  <aside class="mock-sidebar">
    <div class="window-controls" aria-hidden="true"><i></i><i></i><i></i></div>

    <nav class="sidebar-actions" aria-label="Preview navigation">
      <div class="sidebar-action">
        <svg viewBox="0 0 24 24"><circle cx="11" cy="11" r="7"></circle><path d="m20 20-4-4"></path></svg>
        <span>Search</span><kbd>⌘K</kbd>
      </div>
      <div class="sidebar-action">
        <svg viewBox="0 0 24 24"><path d="M12 5v14M5 12h14"></path></svg>
        <span>Project</span>
      </div>
      <div class="sidebar-action">
        <svg viewBox="0 0 24 24"><path d="M5 12h13M13 6l6 6-6 6"></path></svg>
        <span>Classic view</span>
      </div>
    </nav>

    <div class="sidebar-scroll">
      <div class="sidebar-label">
        <span>Projects</span>
        <svg class="section-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"></path></svg>
      </div>
      <div class="project-row active"><b class="project-mark orange">T</b><span>t3code</span></div>
      <div class="project-row"><b class="project-mark purple">D</b><span>Documents</span></div>
      <div class="project-row"><b class="project-mark mint">U</b><span>UICodex</span></div>

      <div class="sidebar-label thread-label">
        <span>t3code’s threads</span>
        <svg class="section-chevron" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="m9 18 6-6-6-6"></path></svg>
      </div>
      <div class="thread-row active"><span>Restore dark theme regressions</span><time>18m</time></div>
      <div class="thread-row plan"><span><strong>Plan Ready</strong>&nbsp; <em>Plan light theme...</em></span><time>29m</time></div>
      <div class="thread-row"><span>Add prompt shortcuts and actions</span><time>50m</time></div>
      <div class="thread-row"><span>First message blinking issue</span><time>54m</time></div>
      <div class="thread-row"><span>Unify folder icon text color</span><time>13h</time></div>
      <div class="thread-row"><span>Code review changes</span><time>13h</time></div>
      <div class="thread-row"><span>Response animation types</span><time>13h</time></div>
      <div class="thread-row"><span>Fix working indicator first blink</span><time>14h</time></div>
      <div class="thread-row"><span>Premium chat streaming animation</span><time>15h</time></div>
      <div class="thread-row"><span>GPT vs Opus comparison</span><time>15h</time></div>
    </div>

    <div class="sidebar-settings">
      <div class="settings-row">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M9.671 4.136a2.34 2.34 0 0 1 4.659 0 2.34 2.34 0 0 0 3.319 1.915 2.34 2.34 0 0 1 2.33 4.033 2.34 2.34 0 0 0 0 3.831 2.34 2.34 0 0 1-2.33 4.033 2.34 2.34 0 0 0-3.319 1.915 2.34 2.34 0 0 1-4.659 0 2.34 2.34 0 0 0-3.32-1.915 2.34 2.34 0 0 1-2.33-4.033 2.34 2.34 0 0 0 0-3.831A2.34 2.34 0 0 1 6.35 6.051a2.34 2.34 0 0 0 3.319-1.915"></path>
          <circle cx="12" cy="12" r="3"></circle>
        </svg>
        <span>Settings</span>
      </div>
    </div>
  </aside>

  <section class="mock-main">
    <header class="mock-header">
      <svg viewBox="0 0 24 24"><rect x="3" y="4" width="18" height="16" rx="3"></rect><path d="M8 4v16"></path></svg>
      <strong>Restore dark theme regressions</strong>
      <span>＋</span>
    </header>

    <div class="conversation">
      <div class="code-card compact">
        <code><span class="orange-text">bg-card</span><span class="blue-text">/70</span><br /><span class="orange-text">from-card</span><span class="blue-text">/95</span> <span class="orange-text">via-card</span><span class="blue-text">/80</span></code>
      </div>

      <p>Current code changed dark to <code class="inline-code">dark:bg-muted</code> / <code class="inline-code">dark:from-muted</code>, which is not “as it was”. Use:</p>

      <div class="code-card">
        <code><span class="orange-text">bg-card</span><span class="blue-text">/70</span> <span class="orange-text">not-dark:bg-</span><span class="yellow-text">[#EFEFEF]</span><span class="blue-text">/95</span><br /><span class="orange-text">from-card</span><span class="blue-text">/95</span> <span class="orange-text">via-card</span><span class="blue-text">/80</span> <span class="orange-text">not-dark:from-</span><span class="yellow-text">[#EFEFEF]</span><span class="blue-text">/95</span></code>
      </div>

      <p>In <a href="#">⚛ ComposerPrimaryActions.tsx · L54</a>, the send button helper changed both themes because the new colors are unscoped. Dark’s previous button was:</p>

      <div class="code-card">
        <code><span class="orange-text">bg-</span><span class="gray-text">[#d4d4d4]</span> <span class="orange-text">text-</span><span class="blue-text">[#171717]</span><br /><span class="orange-text">shadow-</span><span class="purple-text">[inset_0_-1px_1px_rgba(0,0,0,0.17),inset_0_1px_1px_white]</span></code>
      </div>

      <p>So keep those as base, and make the new send colors light-only:</p>
      <div class="code-card last-code">
        <code><span class="green-text">"not-dark:bg-[#c4c4c4] not-dark:text-white ..."</span><br /><span class="orange-text">canSend</span> <span class="pink-text">?</span> <span class="green-text">"enabled:cursor-pointer opacity-100"</span></code>
      </div>
      <time class="message-time">3:36 PM</time>
    </div>

    <div class="composer-wrap">
      <div class="composer">
        <div class="composer-placeholder">Ask anything, @tag files/folders, $use skills, or / for commands</div>
        <div class="composer-toolbar">
          <div class="composer-options"><span>＋</span><i></i><span class="composer-branch-icon"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M15 6a9 9 0 0 0-9 9V3"></path><circle cx="18" cy="6" r="3"></circle><circle cx="6" cy="18" r="3"></circle></svg></span><i></i><span>Ask</span><i></i><span>Codex · GPT-5.5</span><i></i><span>Extra High · Fast</span><i></i><span>Full access</span></div>
          <div class="send-group">
            <button aria-label="Send">
              <svg width="14" height="14" viewBox="0 0 14 14" fill="none" aria-hidden="true"><path d="M7 11.5V2.5M7 2.5 3 6.5M7 2.5l4 4"></path></svg>
            </button>
          </div>
        </div>
      </div>
      <div class="branchbar">
        <div class="branchbar-left">
          <span class="context-icon project-context"><b class="project-mark orange">T</b></span>
          <svg class="context-slash" viewBox="0 0 6 12" aria-hidden="true"><path d="M5.25 0.5L0.75 11.5"></path></svg>
          <span class="context-text workspace-context">
            <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M20 20a2 2 0 0 0 2-2V8a2 2 0 0 0-2-2h-7.9a2 2 0 0 1-1.69-.9L9.6 3.9A2 2 0 0 0 7.93 3H4a2 2 0 0 0-2 2v13a2 2 0 0 0 2 2Z"></path></svg>
            <span>Local checkout</span>
          </span>
          <i class="context-separator"></i>
          <span class="context-text branch-name">refactor/ui-theme-f...</span>
        </div>
        <div class="branchbar-actions">
          <span class="context-text open-editor">Open in Zed</span>
          <i class="context-separator"></i>
          <span class="context-icon" aria-label="Right panel">
            <svg viewBox="0 0 16 16" aria-hidden="true"><path class="filled-icon" d="M12.154 2.004C13.739 2.084 15 3.395 15 5v6c0 1.605-1.261 2.916-2.846 2.996L12 14H4c-1.657 0-3-1.343-3-3V5c0-1.657 1.343-3 3-3h8l.154.004ZM4 3.25c-.967 0-1.75.784-1.75 1.75v6c0 .967.783 1.75 1.75 1.75h8c.967 0 1.75-.783 1.75-1.75V5c0-.966-.783-1.75-1.75-1.75H4Zm8 1.125c.345 0 .625.28.625.625v6a.625.625 0 1 1-1.25 0V5c0-.345.28-.625.625-.625Z"></path></svg>
          </span>
          <i class="context-separator"></i>
          <span class="context-icon" aria-label="Terminal">
            <svg viewBox="0 0 16 16" aria-hidden="true"><path d="M4.25 5.25 6.75 8l-2.5 2.75M8.4 10.75H12"></path></svg>
          </span>
          <i class="context-separator"></i>
          <span class="context-icon" aria-label="Diff">
            <svg viewBox="0 0 16 16" aria-hidden="true"><path d="M4 7h8M4 13h8M8 3v8"></path></svg>
          </span>
          <i class="context-separator"></i>
          <span class="context-icon" aria-label="More actions">
            <svg viewBox="0 0 16 16" aria-hidden="true"><circle cx="4" cy="8" r="1"></circle><circle cx="8" cy="8" r="1"></circle><circle cx="12" cy="8" r="1"></circle></svg>
          </span>
        </div>
      </div>
    </div>
  </section>
</div>

<style>
  .app-mockup {
    --app-bg: #131313;
    --app-card: #1e1e1e;
    --app-fg: #f5f5f5;
    --app-muted: rgba(255, 255, 255, 0.5);
    --app-border: rgba(255, 255, 255, 0.09);
    container-type: inline-size;
    width: 100%;
    height: 100%;
    display: grid;
    grid-template-columns: 26% 74%;
    overflow: hidden;
    background: rgba(19, 19, 19, 0.96);
    color: var(--app-fg);
    font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    font-size: clamp(7px, 1.1cqw, 14px);
    line-height: 1.45;
    text-align: left;
  }

  .app-mockup * { box-sizing: border-box; }
  .app-mockup svg { fill: none; stroke: currentColor; stroke-width: 1.6; }

  .mock-sidebar {
    position: relative;
    min-width: 0;
    display: flex;
    flex-direction: column;
    overflow: hidden;
    border-right: 1px solid rgba(255, 255, 255, 0.1);
    background: rgba(255, 255, 255, 0.045);
    backdrop-filter: blur(28px) saturate(1.25);
  }

  .window-controls {
    height: 7.5%;
    min-height: 40px;
    padding: 0 6%;
    display: flex;
    align-items: center;
    gap: 3.3%;
  }
  .window-controls i { width: clamp(7px, 1.25cqw, 14px); aspect-ratio: 1; border-radius: 50%; }
  .window-controls i:nth-child(1) { background: #ff5f57; }
  .window-controls i:nth-child(2) { background: #febc2e; }
  .window-controls i:nth-child(3) { background: #28c840; }

  .sidebar-actions { padding: 8px; display: flex; flex-direction: column; gap: 4px; }
  .sidebar-action { height: 32px; padding: 0 8px; display: flex; align-items: center; gap: 8px; color: rgba(255,255,255,.82); font-size: 14px; line-height: 1.625; }
  .sidebar-action svg { width: 16px; height: 16px; flex: none; color: rgba(255,255,255,.5); }
  .sidebar-action kbd { margin-left: auto; padding: 2px 6px; border-radius: 6px; background: rgba(255,255,255,.06); color: rgba(255,255,255,.5); font-family: inherit; font-size: 12px; }

  .sidebar-scroll { flex: 1; min-height: 0; padding: 0 8px; overflow: hidden; }
  .sidebar-label { height: 32px; padding: 0 8px; display: flex; align-items: center; justify-content: flex-start; gap: 8px; color: rgba(255,255,255,.5); font-size: 14px; line-height: 1.625; }
  .sidebar-label > span { flex: none; white-space: nowrap; }
  .section-chevron { display: block; width: 16px !important; min-width: 16px; max-width: 16px; height: 16px !important; min-height: 16px; max-height: 16px; flex: 0 0 16px; transform: rotate(90deg); stroke-width: 2 !important; stroke-linecap: round; stroke-linejoin: round; transition: transform 150ms ease; }
  .thread-label { margin-top: 12px; }
  .project-row, .thread-row { width: 100%; min-width: 0; border-radius: 8px; }
  .project-row + .project-row, .thread-row + .thread-row { margin-top: 2px; }
  .project-row { height: 32px; padding: 0 8px; display: flex; align-items: center; gap: 8px; color: rgba(255,255,255,.82); font-size: 14px; line-height: 1.625; }
  .project-row.active, .thread-row.active { background: rgba(255,255,255,.06); }
  .project-mark { width: 16px; height: 16px; display: inline-grid; place-items: center; flex: none; border-radius: 4px; font-size: 9px; font-style: normal; }
  .project-mark.orange { color: #ff9f43; background: rgba(255,159,67,.18); }
  .project-mark.purple { color: #ad85ff; background: rgba(173,133,255,.18); }
  .project-mark.mint { color: #72dfc2; background: rgba(114,223,194,.18); }
  .thread-row { height: 32px; padding: 0 8px; display: flex; align-items: center; gap: 6px; color: rgba(255,255,255,.82); font-size: 14px; line-height: 1.625; }
  .thread-row.active { color: rgba(255,255,255,.92); }
  .thread-row > span { min-width: 0; flex: 1; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .thread-row time { color: rgba(255,255,255,.5); font-size: 14px; line-height: 1.625; }
  .thread-row.plan strong { color: #bda1ff; font-weight: 400; }
  .thread-row.plan em { color: rgba(255,255,255,.82); font-style: normal; }
  .sidebar-settings { flex: none; padding: 8px 8px 12px; color: rgba(255,255,255,.82); }
  .settings-row { width: 100%; height: 32px; padding: 0 8px; display: grid !important; grid-template-columns: 16px minmax(0, 1fr); align-items: center; gap: 8px; border-radius: 8px; font-size: 14px; font-weight: 400; line-height: 1.625; }
  .settings-row > span { min-width: 0; white-space: nowrap; }
  .settings-row svg { display: block; width: 16px !important; min-width: 16px; max-width: 16px; height: 16px !important; min-height: 16px; max-height: 16px; color: rgba(255,255,255,.5); stroke-width: 2 !important; stroke-linecap: round; stroke-linejoin: round; }

  .mock-main { position: relative; min-width: 0; overflow: hidden; background: rgba(19,19,19,.98); }
  .mock-header { height: 7.5%; min-height: 40px; padding: 0 3.5%; display: flex; align-items: center; gap: 4%; border-bottom: 1px solid var(--app-border); }
  .mock-header svg { width: 1.18em; height: 1.18em; color: rgba(255,255,255,.48); }
  .mock-header strong { font-size: 1.02em; font-weight: 550; }
  .mock-header > span { margin-left: auto; color: rgba(255,255,255,.58); font-size: 1.5em; font-weight: 200; }

  .conversation { position: absolute; inset: 7.5% 0 19% 0; padding: 3% 3.5% 8%; overflow: hidden; color: rgba(255,255,255,.82); }
  .conversation p { margin: 1.8% 0; font-size: .94em; line-height: 1.55; }
  .conversation a { padding: .12em .38em; border: 1px solid rgba(255,255,255,.12); border-radius: 5px; color: rgba(255,255,255,.82); text-decoration: underline; text-underline-offset: 2px; }
  .inline-code { padding: .12em .38em; border: 1px solid rgba(255,255,255,.1); border-radius: 5px; background: rgba(255,255,255,.04); color: rgba(255,255,255,.9); }
  .code-card { padding: 1.8% 2%; border: 1px solid rgba(255,255,255,.1); border-radius: 11px; background: rgba(255,255,255,.025); box-shadow: inset 0 1px 1px rgba(255,255,255,.02); }
  .code-card.compact { padding-block: 2%; }
  .code-card code { font-family: "SF Mono", "JetBrains Mono", monospace; font-size: .82em; line-height: 1.7; color: #d4d4d4; }
  .orange-text { color: #fb923c; } .blue-text { color: #67d6ff; } .yellow-text { color: #fde047; } .purple-text { color: #c084fc; } .pink-text { color: #fb7185; } .green-text { color: #5ee17a; } .gray-text { color: #d4d4d4; }
  .message-time { display: block; margin-top: 2%; color: rgba(255,255,255,.3); }

  .composer-wrap { position: absolute; z-index: 3; width: calc(100% - 40px); max-width: 832px; bottom: 12px; left: 50%; transform: translateX(-50%); }
  .composer { height: 112px; min-height: 96px; display: flex; flex-direction: column; border-radius: 32px; background: #1e1e1e; box-shadow: inset -1px -1px 1px rgba(255,255,255,.06), inset 1px 1px 1px rgba(255,255,255,.12), 0 4px 14.4px rgba(9,9,9,.08); }
  .composer-placeholder { min-height: 0; flex: 1; padding: 24px 24px 0; color: rgba(255,255,255,.5); font-size: 14px; line-height: 1.625; }
  .composer-toolbar { height: 48px; padding: 0 16px 16px; display: flex; align-items: center; justify-content: space-between; gap: 8px; color: rgba(255,255,255,.5); }
  .composer-options { min-width: 0; flex: 1; display: flex; align-items: center; gap: 2px; overflow: hidden; white-space: nowrap; font-size: 14px; line-height: 1.625; }
  .composer-options > span { height: 32px; padding: 0 8px; display: inline-flex; align-items: center; flex: none; }
  .composer-options > span:first-child, .composer-options > span:nth-of-type(2) { width: 32px; padding: 0; justify-content: center; }
  .composer-branch-icon svg { width: 16px; height: 16px; stroke-width: 2 !important; stroke-linecap: round; stroke-linejoin: round; }
  .composer-options i { width: 1px; height: 12px; flex: none; background: rgba(255,255,255,.06); }
  .send-group { display: flex; align-items: center; }
  .send-group button { width: 32px; height: 32px; display: grid; place-items: center; flex: none; border: 0; border-radius: 50%; background: #d4d4d4; color: #171717; opacity: .4; box-shadow: inset 0 -1px 1px rgba(0,0,0,.17), inset 0 1px 1px #fff; }
  .send-group button svg { width: 14px; height: 14px; stroke: currentColor; stroke-width: 1.8 !important; stroke-linecap: round; stroke-linejoin: round; }
  .branchbar { height: 40px; padding: 4px 16px 4px 12px; display: flex; align-items: center; justify-content: space-between; gap: 8px; color: rgba(255,255,255,.5); font-size: 14px; line-height: 1.625; filter: drop-shadow(0 4px 2px rgba(0,0,0,.25)); }
  .branchbar-left, .branchbar-actions { min-width: 0; display: flex; align-items: center; gap: 0; white-space: nowrap; overflow: hidden; }
  .branchbar-left { flex: 1; }
  .branchbar-actions { flex: none; justify-content: flex-end; }
  .context-icon { width: 32px; height: 32px; display: inline-flex; align-items: center; justify-content: center; flex: none; }
  .context-icon svg { width: 16px; height: 16px; color: rgba(255,255,255,.5); stroke-width: 1.3 !important; stroke-linecap: round; stroke-linejoin: round; }
  .context-icon .filled-icon { fill: currentColor; stroke: none; }
  .context-icon circle { fill: currentColor; stroke: none; }
  .project-context .project-mark { width: 16px; height: 16px; }
  .context-slash { width: 6px; height: 12px; flex: none; color: rgba(255,255,255,.06); }
  .context-slash path { stroke-width: 1 !important; }
  .context-text { height: 32px; min-width: 0; padding: 0 8px; display: inline-flex; align-items: center; gap: 4px; flex: none; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
  .context-text svg { width: 14px; height: 14px; flex: none; stroke-width: 2 !important; stroke-linecap: round; stroke-linejoin: round; }
  .branch-name { max-width: 180px; flex: 1; }
  .context-separator { width: 1px; height: 12px; flex: none; background: rgba(255,255,255,.06); }
  .open-editor { max-width: 144px; }

  @container (max-width: 720px) {
    .app-mockup { grid-template-columns: 30% 70%; }
    .sidebar-actions, .sidebar-scroll { padding-left: 6px; padding-right: 6px; }
    .sidebar-action, .sidebar-label, .project-row, .thread-row { height: 24px; padding-left: 6px; padding-right: 6px; gap: 6px; font-size: 10px; }
    .project-mark, .sidebar-action svg { width: 12px; height: 12px; }
    .section-chevron { width: 12px !important; min-width: 12px; max-width: 12px; height: 12px !important; min-height: 12px; max-height: 12px; flex-basis: 12px; }
    .thread-row time { font-size: 10px; }
    .thread-label { margin-top: 8px; }
    .sidebar-settings { padding: 6px 6px 8px; }
    .settings-row { height: 24px; padding: 0 6px; grid-template-columns: 12px minmax(0, 1fr); gap: 6px; font-size: 10px; }
    .settings-row svg { width: 12px !important; min-width: 12px; max-width: 12px; height: 12px !important; min-height: 12px; max-height: 12px; }
    .conversation { padding-inline: 4%; }
    .composer-wrap { width: calc(100% - 24px); bottom: 8px; }
    .composer { height: 84px; min-height: 72px; border-radius: 24px; }
    .composer-placeholder { padding: 14px 14px 0; font-size: 10px; }
    .composer-toolbar { height: 36px; padding: 0 10px 10px; }
    .composer-options { font-size: 10px; }
    .composer-options > span { height: 24px; padding: 0 6px; }
    .composer-options > span:first-child, .composer-options > span:nth-of-type(2) { width: 24px; }
    .send-group button { width: 24px; height: 24px; }
    .send-group button svg { width: 11px; height: 11px; }
    .branchbar { height: 30px; padding: 3px 8px; font-size: 10px; }
    .context-icon, .context-text { height: 24px; }
    .context-icon { width: 24px; }
    .context-icon svg { width: 12px; height: 12px; }
    .context-text { padding: 0 6px; }
    .context-text svg { width: 11px; height: 11px; }
    .branchbar-actions .open-editor, .branchbar-actions .context-separator:first-of-type, .branchbar-actions .context-icon:nth-of-type(1), .branchbar-actions .context-separator:nth-of-type(2) { display: none; }
    .composer-options span:nth-of-type(n+4), .composer-options i:nth-of-type(n+4) { display: none; }
  }
</style>
